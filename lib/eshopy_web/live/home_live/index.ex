defmodule EshopyWeb.HomeLive.Index do
  use EshopyWeb, :live_view

  @impl true
  def mount(_params, _session, socket) do
    {:ok, socket}
  end

  # @impl true
  # def handle_event() do

  # end
end
